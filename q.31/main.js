let username = ["umer", "usman", "admin", "abubakar", "mustufa"];
if (username.length === 0) {
    console.log("We need to find some users");
}
else {
    username.forEach(username => console.log(`hello ${username}`));
}
username = [];
if (username.length === 0) {
    console.log("We need to find some users");
}
else {
    username.forEach(username => console.log(`hello ${username}`));
}
export {};
